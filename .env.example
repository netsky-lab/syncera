# Copy to .env. Bun auto-loads .env (no dotenv package needed).

# ---- LLM endpoint (OpenAI-compatible HTTP) ----
# Choose one: qwen (self-hosted/OpenAI-compatible) or gemini.
LLM_PROVIDER=qwen
# Set to 0 for llama.cpp / Runpod proxies that break SSE streams.
LLM_STREAM=0
# Structured outputs always use OpenAI-compatible function/tool calling.

# Qwen/self-hosted provider. GEMMA_* is legacy fallback; prefer QWEN_*.
QWEN_BASE_URL=http://localhost:8080/v1
QWEN_MODEL=qwen3.6-35b-a3b
QWEN_API_KEY=dummy
GEMMA_BASE_URL=http://localhost:8080/v1
GEMMA_MODEL=qwen3.6-35b-a3b
GEMMA_API_KEY=dummy

# Gemini provider: OpenAI-compatible chat plus native Gemini tools.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3-flash-preview
GEMINI_REASONING_EFFORT=low
GEMINI_SEARCH_GROUNDING=0
GEMINI_SEARCH_MAX_RESULTS=5
GEMINI_SEARCH_TIMEOUT_MS=20000

# Optional cost model override. Defaults: Gemini input=$0.50/1M,
# Gemini output=$3.00/1M, self-hosted Qwen=$0 unless overridden.
# LLM_INPUT_USD_PER_1M=0.5
# LLM_OUTPUT_USD_PER_1M=3

# Optional provider-aware concurrency overrides. Defaults:
# qwen: harvest=3 evidence=3 analyzer=4 relevance=5 verifier=5
# gemini: harvest=6 evidence=8 analyzer=8 relevance=12 verifier=12
# Stable Qwen 16-slot / 64k starting profile:
# LLM_MAX_CONCURRENCY=8
# CONCURRENCY_HARVEST=6
# CONCURRENCY_EVIDENCE=2
# CONCURRENCY_ANALYZER=1
# CONCURRENCY_RELEVANCE=6
# CONCURRENCY_VERIFIER=6
# EVIDENCE_LEARNINGS_PER_BATCH=2
# EVIDENCE_MAX_TOKENS=4096

# ---- Deep research harvest budget ----
# Defaults target a real deep-research corpus, not a quick demo.
# HARVEST_BREADTH=8
# HARVEST_DEPTH=2
# HARVEST_PAGES_PER_QUERY=3
# HARVEST_URLS_PER_QUERY=10
# HARVEST_READ_CONCURRENCY=6
# MAX_HARVEST_MINUTES=90
# MAX_HARVEST_SOURCES=400
# SNOWBALL_MAX_PAPERS=8

# Comma-separated list of fallback base URLs. If a request to QWEN_BASE_URL
# fails (e.g. Runpod pod bounced), llm.ts retries against each in order.
QWEN_FALLBACK_URLS=

# ---- Web app security defaults (used by apps/web) ----
# Set in production deployments. Required for durable sessions.
SESSION_SECRET=
# Optional first-admin bootstrap token when using public signup bootstrap.
BOOTSTRAP_TOKEN=
# Empty = fail-closed. Set to comma-separated origins for cross-origin API use.
API_CORS_ORIGINS=

# ---- SearXNG self-hosted (see infra/searxng/) ----
SEARXNG_URL=http://localhost:8888
# URL visible from spawned pipeline containers. In local dev with
# infra/searxng/docker-compose.yml this is usually http://searxng-core:8080.
# In deploy compose this is usually http://searxng:8080.
PIPELINE_SEARXNG_URL=

# ---- Playwright page extraction ----
# Optional: path to a system chromium so Playwright skips its own download.
# PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/usr/bin/chromium-browser
